{
  "name": "claude-sub-proxy",
  "version": "2.0.0",
  "type": "module",
  "description": "Local HTTP API server powered by Claude Code Agent SDK — use your Claude subscription via standard Messages API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "keywords": [
    "claude",
    "anthropic",
    "agent-sdk",
    "proxy",
    "subscription",
    "claude-code"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  }
}
