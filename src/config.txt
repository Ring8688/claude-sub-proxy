# Claude Sub Proxy Configuration
port=42069
host=
log_level=INFO

# Default model when not specified in the request
model_default=claude-sonnet-4-6

# API key for proxy authentication (leave empty for open access)
proxy_api_key=
